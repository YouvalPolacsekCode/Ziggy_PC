# Core AI + Speech
openai>=1.0.0
faster-whisper
SpeechRecognition
pyaudio
langdetect
gTTS
playsound==1.2.2
pvporcupine 
sounddevice
onnxruntime
openwakeword

# Dashboard
Flask

# Web push notifications
pywebpush>=2.0.0

# System, Files, Scheduling
psutil
watchdog
pyyaml
python-dotenv
pytz
dateparser

# MQTT (optional – comment if unused)
paho-mqtt

# Zigbee / Home Assistant Support (optional – comment if unused)
zigpy
bellows
zigpy-znp
zha-quirks
RPi.GPIO; platform_system == "Linux"
gpiozero; platform_system == "Linux"

# FFmpeg and media support
ffmpeg-python

# File generation support (optional – future features)
python-docx
python-pptx
openpyxl
reportlab  # for basic PDFs
dotenv
PyYAML
numpy

#web-app
flask
flask-cors
fastapi>=0.100.0
uvicorn[standard]>=0.23.0
aiofiles>=23.0.0
python-multipart>=0.0.6
websockets>=11.0
aiosqlite>=0.19.0

# Password hashing — bcrypt for the auth.db user store. Pinned to match the
# relay (relay/requirements.txt) so both surfaces ship identical hash output.
# bcrypt is pinned to <4.1 because passlib 1.7.4 (last upstream release, 2020)
# reads bcrypt.__about__ which was removed in bcrypt 4.1; bcrypt 5.0 also added
# a 72-byte length check that breaks passlib's startup self-test.
passlib[bcrypt]==1.7.4
bcrypt<4.1

# IR blaster direct access (used alongside HA — enables continuous IR receive)
broadlink>=0.18.0

# Switcher account credential validation. HA's switcher_kis integration uses
# this same library internally for protocol work; we only use the account
# helpers (validate_token) to verify+cache the user's Switcher credentials
# inside Ziggy UI so every subsequent device pairing is one-click.
aioswitcher>=6.0

# Testing
pytest>=8.0
pytest-asyncio>=0.23

ddgs>=0.1.0
feedparser>=6.0.11
recipe-scrapers>=14.55.0
trafilatura>=1.9.0
requests>=2.32.3
yfinance>=0.2.40
spotipy>=2.24.0
google-api-python-client>=2.136.0
google-auth-httplib2>=0.2.0
google-auth-oauthlib>=1.2.0
