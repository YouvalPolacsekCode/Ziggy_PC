fastapi==0.115.0
uvicorn[standard]==0.30.6
aiosqlite==0.20.0
PyJWT==2.9.0
httpx==0.27.2
python-multipart==0.0.12
python-dotenv==1.0.1
docker==7.1.0
passlib[bcrypt]==1.7.4
# passlib 1.7.4 predates bcrypt 4.x's stricter 72-byte handling; unpinned pip
# resolves to bcrypt 5.x which breaks passlib's init-time detect_wrap_bug()
# call. All login attempts against bcrypt-hashed rows return 401 silently
# because verify_password() catches the ValueError and returns False.
bcrypt<4.0
# asyncssh removed in Phase 5: Oracle-VM SSH provisioning path is deleted.
# provisioner.py only hits the Cloudflare API via httpx now.
# cryptography was a transitive dep of asyncssh but is still directly used
# by relay/app/routers/backup_keys.py — pin it explicitly so removing
# asyncssh doesn't take it out.
cryptography>=41.0.0
stripe==11.1.0
boto3>=1.34.0
# WebSocket client for the per-home realtime proxy (relay/app/routers/proxy.py).
# The relay dials each hub's /ws over the tunnel and pumps frames both ways.
# uvicorn[standard] already bundles websockets for the *server* side; pinning it
# here makes the *client* dependency explicit. 16.x provides the asyncio client
# (websockets.asyncio.client.connect) this code imports.
websockets==16.0

